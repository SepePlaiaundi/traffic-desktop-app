<Window x:Class="TrafficDesktopApp.Windows.Users"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:general="clr-namespace:TrafficDesktopApp.Controls.General"
        xmlns:controls="clr-namespace:TrafficDesktopApp.Controls.Users"
        Title="SEID - Usuarios"
        MinWidth="1100"
        MinHeight="720"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized">


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <general:Header x:Name="Header" Grid.Row="0" Title="Usuarios"/>

        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">

            <Grid Margin="20">
                <Border Style="{StaticResource CardBorderElevated}">

                    <controls:UsersListView x:Name="UsersList" ClipToBounds="True"/>
                </Border>
            </Grid>
        </ScrollViewer>
    </Grid>
</Window>