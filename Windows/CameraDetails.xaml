<Window x:Class="TrafficDesktopApp.Windows.CameraDetails"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SEID - Detalle de Cámara" 
        Height="620" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#F7F7F7" 
        ResizeMode="NoResize">

    <Window.Resources>
        <Style x:Key="ModernSwitchStyle" TargetType="ToggleButton">
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Grid>
                            <Border x:Name="Track" 
                                    Background="#E0E0E0" 
                                    CornerRadius="12" 
                                    BorderThickness="0"/>

                            <Ellipse x:Name="Thumb" 
                                     Fill="White" 
                                     Width="18" Height="18" 
                                     HorizontalAlignment="Left" 
                                     Margin="3,0">
                                <Ellipse.RenderTransform>
                                    <TranslateTransform X="0"/>
                                </Ellipse.RenderTransform>
                                <Ellipse.Effect>
                                    <DropShadowEffect BlurRadius="4" ShadowDepth="1" Opacity="0.2"/>
                                </Ellipse.Effect>
                            </Ellipse>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Track" Property="Background" Value="#2ecc71"/>
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="Thumb" 
                                                             Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)" 
                                                             To="22" Duration="0:0:0.15"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="Thumb" 
                                                             Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)" 
                                                             To="0" Duration="0:0:0.15"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="320"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Margin="24" Background="White" 
                CornerRadius="8" BorderBrush="#E0E0E0" BorderThickness="1">

            <Grid Background="Transparent">

                <Image Source="{Binding Image}" Stretch="Uniform" Margin="1"/>

                <Border HorizontalAlignment="Left" VerticalAlignment="Top" Margin="16"
                        Background="#CC222222" CornerRadius="4" Padding="10,5"
                        IsHitTestVisible="False">
                    <StackPanel Orientation="Horizontal">
                        <Ellipse Width="8" Height="8" Fill="#00E676" Margin="0,0,8,0"/>
                        <TextBlock Text="CÁMARA ACTIVA" Foreground="White" FontSize="11" FontWeight="Bold"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <Border Grid.Column="1" Background="White" BorderBrush="#E0E0E0" BorderThickness="1,0,0,0">
            <Grid Margin="24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0">
                    <TextBlock Text="ESPECIFICACIONES" Foreground="#9E9E9E" FontSize="10" FontWeight="Bold" Margin="0,0,0,6"/>
                    <TextBlock Text="{Binding CameraName}" Foreground="#222222" FontSize="22" FontWeight="Bold" TextWrapping="Wrap"/>
                    <Border Height="3" Width="30" Background="#222222" HorizontalAlignment="Left" Margin="0,12,0,20"/>
                </StackPanel>

                <StackPanel Grid.Row="1">
                    <TextBlock Text="CARRETERA / VÍA" Foreground="#9E9E9E" FontSize="10" FontWeight="Bold" Margin="0,0,0,4"/>
                    <TextBlock Text="{Binding Road}" Foreground="#444444" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,20"/>

                    <UniformGrid Columns="2" Margin="0,0,0,24">
                        <StackPanel>
                            <TextBlock Text="LATITUD" Foreground="#9E9E9E" FontSize="10" FontWeight="Bold"/>
                            <TextBlock Text="{Binding Latitude}" Foreground="#666666" FontSize="14" Margin="0,2,0,0"/>
                        </StackPanel>
                        <StackPanel>
                            <TextBlock Text="LONGITUD" Foreground="#9E9E9E" FontSize="10" FontWeight="Bold"/>
                            <TextBlock Text="{Binding Longitude}" Foreground="#666666" FontSize="14" Margin="0,2,0,0"/>
                        </StackPanel>
                    </UniformGrid>

                    <Border Background="#F9F9F9" Padding="16" CornerRadius="6" BorderBrush="#E0E0E0" BorderThickness="1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel>
                                <TextBlock Text="ESTADO" FontSize="10" FontWeight="Bold" Foreground="#9E9E9E" Margin="0,0,0,2"/>
                                <TextBlock Text="Visibilidad en Red" FontSize="13" Foreground="#444"/>
                            </StackPanel>

                            <ToggleButton Grid.Column="1" 
                                  Style="{StaticResource ModernSwitchStyle}" 
                                  IsChecked="{Binding IsActive, Mode=OneWay}" 
                                  Cursor="Hand"
                                  VerticalAlignment="Center"
                                  Click="EstadoSwitch_Click"/>
                        </Grid>
                    </Border>
                </StackPanel>

                <StackPanel Grid.Row="2">
                    <Button Content="Eliminar Cámara" Height="40" Margin="0,0,0,10"
                            Background="Transparent" Foreground="#E74C3C" FontWeight="SemiBold" 
                            BorderBrush="#E74C3C" BorderThickness="1" Cursor="Hand" 
                            Click="DeleteButton_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                    BorderThickness="{TemplateBinding BorderThickness}" 
                                    CornerRadius="4">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <Button Content="Cerrar Detalle" Height="40" 
                            Background="#222222" Foreground="White" FontWeight="Bold" 
                            BorderThickness="0" Cursor="Hand" Click="CloseButton_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>