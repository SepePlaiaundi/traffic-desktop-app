<Window x:Class="TrafficDesktopApp.Windows.CreateIncidenceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SEID - Nueva Incidencia"
        Width="600"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F9FAFB"
        WindowStyle="None"
        AllowsTransparency="True">

    <Window.Resources>
        <Style x:Key="ModernInput" TargetType="{x:Type Control}">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Control}">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <Grid>
                                <ScrollViewer x:Name="PART_ContentHost" 
                                              Focusable="false" 
                                              HorizontalScrollBarVisibility="Hidden" 
                                              VerticalScrollBarVisibility="Hidden"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#D1D5DB"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#F5B400"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox" BasedOn="{StaticResource ModernInput}"/>
        <Style TargetType="DatePicker">
            <Setter Property="Height" Value="40"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        
        <Style x:Key="LabelText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>
    </Window.Resources>

    <Border BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="12" Background="White" Margin="15"
            MouseLeftButtonDown="Border_MouseLeftButtonDown">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" Direction="270" Opacity="0.15" ShadowDepth="10"/>
        </Border.Effect>

        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Header -->
                <RowDefinition Height="Auto"/> <!-- Form -->
                <RowDefinition Height="Auto"/> <!-- Error Area -->
                <RowDefinition Height="Auto"/> <!-- Actions -->
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,24">
                <TextBlock Text="Crear Nueva Incidencia" 
                           FontSize="22" 
                           FontWeight="Bold" 
                           Foreground="#111827"/>
                <TextBlock Text="Introduce los detalles de la incidencia de tráfico." 
                           FontSize="14" 
                           Foreground="#6B7280" 
                           Margin="0,4,0,0"/>
            </StackPanel>

            <!-- Form -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Col 0 -->
                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,0,15">
                    <TextBlock Text="Causa" Style="{StaticResource LabelText}"/>
                    <TextBox x:Name="CauseBox"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,0,15">
                    <TextBlock Text="Carretera" Style="{StaticResource LabelText}"/>
                    <TextBox x:Name="RoadBox"/>
                </StackPanel>

                <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,0,15">
                    <TextBlock Text="Ciudad" Style="{StaticResource LabelText}"/>
                    <TextBox x:Name="CityBox"/>
                </StackPanel>

                <StackPanel Grid.Row="3" Grid.Column="0" Margin="0,0,0,15">
                    <TextBlock Text="Latitud" Style="{StaticResource LabelText}"/>
                    <TextBox x:Name="LatBox"/>
                </StackPanel>

                <StackPanel Grid.Row="4" Grid.Column="0" Margin="0,0,0,15">
                    <TextBlock Text="Tipo" Style="{StaticResource LabelText}"/>
                    <TextBox x:Name="TypeBox"/>
                </StackPanel>

                <StackPanel Grid.Row="5" Grid.Column="0" Margin="0,0,0,15">
                    <TextBlock Text="Fecha Inicio" Style="{StaticResource LabelText}"/>
                    <DatePicker x:Name="StartDatePicker"/>
                </StackPanel>

                <!-- Col 2 -->
                <StackPanel Grid.Row="0" Grid.Column="2" Margin="0,0,0,15">
                    <TextBlock Text="Nivel" Style="{StaticResource LabelText}"/>
                    <TextBox x:Name="LevelBox"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="2" Margin="0,0,0,15">
                    <TextBlock Text="Dirección" Style="{StaticResource LabelText}"/>
                    <TextBox x:Name="DirectionBox"/>
                </StackPanel>

                <StackPanel Grid.Row="2" Grid.Column="2" Margin="0,0,0,15">
                    <TextBlock Text="Provincia" Style="{StaticResource LabelText}"/>
                    <TextBox x:Name="ProvinceBox"/>
                </StackPanel>

                <StackPanel Grid.Row="3" Grid.Column="2" Margin="0,0,0,15">
                    <TextBlock Text="Longitud" Style="{StaticResource LabelText}"/>
                    <TextBox x:Name="LngBox"/>
                </StackPanel>

                <StackPanel Grid.Row="4" Grid.Column="2" Margin="0,0,0,15">
                    <TextBlock Text="ID Recurso" Style="{StaticResource LabelText}"/>
                    <TextBox x:Name="ResourceIdBox" Text="1"/> <!-- Default ID -->
                </StackPanel>

                <StackPanel Grid.Row="5" Grid.Column="2" Margin="0,0,0,15">
                    <TextBlock Text="Fecha Fin" Style="{StaticResource LabelText}"/>
                    <DatePicker x:Name="EndDatePicker"/>
                </StackPanel>

                <!-- Full Width -->
                <StackPanel Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,0,0,15">
                    <TextBlock Text="Descripción" Style="{StaticResource LabelText}"/>
                    <TextBox x:Name="DescriptionBox" Height="80" VerticalContentAlignment="Top" Padding="10" AcceptsReturn="True" TextWrapping="Wrap"/>
                </StackPanel>
            </Grid>

            <!-- Error Area -->
            <Border x:Name="ErrorBorder" 
                    Grid.Row="2" 
                    Background="#FEF2F2" 
                    BorderBrush="#FEE2E2" 
                    BorderThickness="1" 
                    CornerRadius="8" 
                    Padding="12" 
                    Margin="0,15,0,0"
                    Visibility="Collapsed">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="⚠" Foreground="#DC2626" FontSize="16" FontWeight="Bold" Margin="0,-2,10,0"/>
                    <TextBlock x:Name="ErrorText" 
                               Grid.Column="1"
                               Text="" 
                               Foreground="#B91C1C" 
                               FontSize="13" 
                               TextWrapping="Wrap"
                               VerticalAlignment="Center"/>
                </Grid>
            </Border>

            <!-- Actions -->
            <Grid Grid.Row="3" Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>

                <Button Content="Cancelar" 
                        IsCancel="True"
                        Height="40"
                        Background="Transparent"
                        Foreground="#6B7280"
                        BorderThickness="0"
                        Cursor="Hand"
                        FontWeight="Medium">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#F3F4F6"/>
                                    <Setter Property="Foreground" Value="#111827"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <Button Grid.Column="1"
                        Content="Crear Incidencia"
                        IsDefault="True"
                        Click="Create_Click"
                        Height="40"
                        Background="#F5B400"
                        Foreground="Black"
                        BorderThickness="0"
                        Cursor="Hand"
                        FontWeight="Bold"
                        Margin="12,0,0,0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#E5A800"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Grid>
    </Border>
</Window>
