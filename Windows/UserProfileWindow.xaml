<Window x:Class="TrafficDesktopApp.Windows.UserProfileWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Mi Perfil"
        Width="460"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F9FAFB"
        WindowStyle="None"
        AllowsTransparency="True"
        Loaded="Window_Loaded">

    <Window.Resources>
        <Style x:Key="ModernInput" TargetType="{x:Type Control}">
            <Setter Property="Height" Value="45"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Control}">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <Grid>
                                <ScrollViewer x:Name="PART_ContentHost" 
                                              Focusable="false" 
                                              HorizontalScrollBarVisibility="Hidden" 
                                              VerticalScrollBarVisibility="Hidden"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#D1D5DB"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#F5B400"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox" BasedOn="{StaticResource ModernInput}"/>
        <Style TargetType="PasswordBox" BasedOn="{StaticResource ModernInput}"/>

        <Style x:Key="LabelText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
    </Window.Resources>

    <Border BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="12" Background="White" Margin="15"
            MouseLeftButtonDown="Border_MouseLeftButtonDown">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" Direction="270" Opacity="0.15" ShadowDepth="10"/>
        </Border.Effect>

        <Grid Margin="32">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Header -->
                <RowDefinition Height="Auto"/> <!-- Form -->
                <RowDefinition Height="Auto"/> <!-- Error Area -->
                <RowDefinition Height="Auto"/> <!-- Actions -->
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,24">
                <TextBlock Text="Mi Perfil" 
                           FontSize="24" 
                           FontWeight="Bold" 
                           Foreground="#111827"/>
                <TextBlock Text="Actualiza tu información personal y contraseña." 
                           FontSize="14" 
                           Foreground="#6B7280" 
                           Margin="0,4,0,0"
                           TextWrapping="Wrap"/>
            </StackPanel>

            <!-- Form -->
            <StackPanel Grid.Row="1">
                <Grid Margin="0,0,0,24">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <Border Width="80" Height="80" CornerRadius="40" Background="#F3F4F6" ClipToBounds="True">
                        <Grid>
                            <Ellipse Fill="#E5E7EB"/>
                            <TextBlock x:Name="AvatarInitials" Text="U" FontSize="32" FontWeight="Bold" Foreground="#9CA3AF" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            <Image x:Name="AvatarImage" Stretch="UniformToFill" Visibility="Collapsed"/>
                            <Button VerticalAlignment="Bottom" Height="24" Background="#80000000" BorderThickness="0" Click="SelectAvatar_Click">
                                <TextBlock Text="Cambiar" FontSize="10" Foreground="White" HorizontalAlignment="Center"/>
                            </Button>
                        </Grid>
                    </Border>
                    
                    <StackPanel Grid.Column="1" Margin="20,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="URL del Avatar (Opcional)" Style="{StaticResource LabelText}"/>
                        <TextBox x:Name="AvatarUrlBox" TextChanged="AvatarUrlBox_TextChanged"/>
                    </StackPanel>
                </Grid>

                <TextBlock Text="Nombre Completo" Style="{StaticResource LabelText}"/>
                <TextBox x:Name="NameBox" Margin="0,0,0,20"/>

                <TextBlock Text="Correo Electrónico (No editable)" Style="{StaticResource LabelText}"/>
                <TextBox x:Name="EmailBox" IsReadOnly="True" Background="#F9FAFB" Margin="0,0,0,20"/>

                <TextBlock Text="Nueva Contraseña (Dejar en blanco para no cambiar)" Style="{StaticResource LabelText}"/>
                <PasswordBox x:Name="PasswordBox" Margin="0,0,0,0"/>
            </StackPanel>

            <!-- Error Area -->
            <Border x:Name="ErrorBorder" 
                    Grid.Row="2" 
                    Background="#FEF2F2" 
                    BorderBrush="#FEE2E2" 
                    BorderThickness="1" 
                    CornerRadius="8" 
                    Padding="12" 
                    Margin="0,24,0,0"
                    Visibility="Collapsed">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="⚠" Foreground="#DC2626" FontSize="16" FontWeight="Bold" Margin="0,-2,10,0"/>
                    <TextBlock x:Name="ErrorText" 
                               Grid.Column="1"
                               Text="" 
                               Foreground="#B91C1C" 
                               FontSize="13" 
                               TextWrapping="Wrap"
                               VerticalAlignment="Center"/>
                </Grid>
            </Border>

            <!-- Actions -->
            <Grid Grid.Row="3" Margin="0,32,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>

                <Button Content="Cerrar" 
                        Click="Close_Click"
                        Height="45"
                        Background="Transparent"
                        Foreground="#6B7280"
                        BorderThickness="0"
                        Cursor="Hand"
                        FontWeight="Medium">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#F3F4F6"/>
                                    <Setter Property="Foreground" Value="#111827"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <Button Grid.Column="1"
                        Content="Guardar Cambios"
                        IsDefault="True"
                        Click="Save_Click"
                        Height="45"
                        Background="#F5B400"
                        Foreground="Black"
                        BorderThickness="0"
                        Cursor="Hand"
                        FontWeight="Bold"
                        Margin="12,0,0,0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#E5A800"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Grid>
    </Border>
</Window>
