<Window x:Class="TrafficDesktopApp.Incidents"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:TrafficDesktopApp.Controls.Incidents"
        mc:Ignorable="d"
        Title="Incidents" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <controls:IncidentsSidebar x:Name="Sidebar"/>

        <controls:IncidentsListView x:Name="IncidentsList" Grid.Column="2"/>
    </Grid>

</Window>
