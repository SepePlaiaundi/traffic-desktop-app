<UserControl x:Class="TrafficDesktopApp.Controls.Users.UsersListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Border Background="White"
            BorderBrush="#DDD"
            BorderThickness="1"
            CornerRadius="6"
            Padding="16">

        <DockPanel>

            <TextBlock Text="Todos los usuarios"
                       FontSize="14"
                       FontWeight="SemiBold"
                       DockPanel.Dock="Top"
                       Margin="0,0,0,10"/>

            <DataGrid ItemsSource="{Binding VisibleUsers}"
                      AutoGenerateColumns="False"
                      HeadersVisibility="Column"
                      GridLinesVisibility="None"
                      CanUserAddRows="False">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nombre y Apellidos" Binding="{Binding Name}" Width="60"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
                </DataGrid.Columns>

            </DataGrid>

        </DockPanel>
    </Border>
</UserControl>
