<UserControl x:Class="TrafficDesktopApp.Controls.Users.UsersListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignWidth="800">

    <UserControl.Resources>
        <Style x:Key="EditableNameStyle" TargetType="TextBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="Padding" Value="2,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="{DynamicResource InputBorderHoverBrush}"/>
                    <Setter Property="Background" Value="{DynamicResource HoverBackgroundBrush}"/>
                </Trigger>
                <Trigger Property="IsKeyboardFocused" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource InputFocusBrush}"/> <Setter Property="BorderThickness" Value="0,0,0,2"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="0,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="*"/>    </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                <StackPanel>
                    <TextBlock Text="Gestión de Usuarios" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                    <TextBlock Text="Lista completa de usuarios y administradores" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="13" Margin="0,2,0,0"/>
                </StackPanel>
                
                <Button Style="{StaticResource RefreshButtonStyle}" 
                        Margin="20,0,0,0"
                        Click="Refresh_Click"
                        ToolTip="Recargar lista"/>
            </StackPanel>

            <Button Grid.Column="1" Click="CreateUser_Click" Style="{StaticResource DarkButton}" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="+ NUEVO USUARIO" FontSize="12"/>
                </StackPanel>
            </Button>
        </Grid>

        <Border Grid.Row="1" Background="{DynamicResource HoverBackgroundBrush}" BorderBrush="{DynamicResource BorderLightBrush}" BorderThickness="0,1" Padding="12,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>  <ColumnDefinition Width="2*"/>  <ColumnDefinition Width="150"/> <ColumnDefinition Width="140"/> </Grid.ColumnDefinitions>

                <TextBlock Text="USUARIO" FontWeight="SemiBold" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}"/>
                <TextBlock Text="EMAIL" Grid.Column="1" FontWeight="SemiBold" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}"/>
                <TextBlock Text="ROL" Grid.Column="2" FontWeight="SemiBold" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}"/>
                <TextBlock Text="ACCIONES" Grid.Column="3" FontWeight="SemiBold" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" HorizontalAlignment="Right" Margin="0,0,10,0"/>
            </Grid>
        </Border>

        <ItemsControl Grid.Row="2" ItemsSource="{Binding VisibleUsers}" Margin="0,4,0,0">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="#F3F4F6" BorderThickness="0,0,0,1" Padding="12,16">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="140"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Border Width="32" Height="32" CornerRadius="16" Background="#FDE68A" Margin="0,0,12,0">
                                    <TextBlock Text="{Binding NombreCompleto[0]}" 
                                               HorizontalAlignment="Center" VerticalAlignment="Center" 
                                               FontWeight="Bold" Foreground="#92400E" FontSize="12"/>
                                </Border>
                                
                                <TextBox Text="{Binding NombreCompleto, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource EditableNameStyle}"
                                         FontWeight="SemiBold"
                                         Foreground="{DynamicResource TextPrimaryBrush}"
                                         FontSize="14"
                                         VerticalAlignment="Center"
                                         Width="200"
                                         HorizontalAlignment="Left"/>
                            </StackPanel>

                            <TextBlock Grid.Column="1" 
                                       Text="{Binding Email}"
                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                       FontSize="13"
                                       VerticalAlignment="Center"
                                       TextTrimming="CharacterEllipsis"
                                       Padding="0"/>

                            <ComboBox Grid.Column="2"
                                      ItemsSource="{Binding DataContext.AvailableRoles, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                      DisplayMemberPath="Description"
                                      SelectedValuePath="Name"
                                      SelectedValue="{Binding Rol, UpdateSourceTrigger=PropertyChanged}"
                                      BorderThickness="1"
                                      BorderBrush="#D1D5DB"
                                      Background="White"
                                      VerticalAlignment="Center"
                                      Height="32"
                                      FontSize="13">
                                <ComboBox.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="4"/>
                                    </Style>
                                </ComboBox.Resources>
                            </ComboBox>

                            <Button Grid.Column="3"
                                    Content="Guardar"
                                    Click="SaveUser_Click"
                                    HorizontalAlignment="Right"
                                    Style="{StaticResource PrimaryButton}"
                                    FontSize="12"
                                    IsEnabled="{Binding HasChanges}"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>