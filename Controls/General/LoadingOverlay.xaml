<UserControl x:Class="TrafficDesktopApp.Controls.General.LoadingOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>
    
    <Grid Background="#80FFFFFF" 
          Visibility="{Binding IsLoading, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BoolToVis}}"
          d:Visibility="Visible">
        
        <Border Background="White" 
                CornerRadius="8" 
                Padding="20"
                HorizontalAlignment="Center" 
                VerticalAlignment="Center"
                Effect="{StaticResource DropShadowEffect}">
            <StackPanel>
                <!-- Modern Spinner -->
                <ProgressBar IsIndeterminate="True" 
                             Width="150" 
                             Height="4" 
                             BorderThickness="0"
                             Foreground="{StaticResource AccentBrush}"
                             Background="#EEE"/>
                
                <TextBlock Text="Cargando..." 
                           HorizontalAlignment="Center" 
                           Margin="0,10,0,0"
                           FontWeight="SemiBold"
                           FontSize="14"
                           Foreground="{StaticResource TextPrimaryBrush}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
