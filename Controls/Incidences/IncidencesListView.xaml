<UserControl x:Class="TrafficDesktopApp.Controls.Incidences.IncidencesListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Border Background="White"
            BorderBrush="#DDD"
            BorderThickness="1"
            CornerRadius="6"
            Padding="16">

        <DockPanel>

            <TextBlock Text="Todas las incidencias"
                       FontSize="14"
                       FontWeight="SemiBold"
                       DockPanel.Dock="Top"
                       Margin="0,0,0,10"/>

            <DataGrid ItemsSource="{Binding VisibleIncidents}"
                      AutoGenerateColumns="False"
                      HeadersVisibility="Column"
                      GridLinesVisibility="None"
                      CanUserAddRows="False">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                    <DataGridTextColumn Header="Descripción" Binding="{Binding Description}" Width="*"/>
                    <DataGridTextColumn Header="Carretera" Binding="{Binding Road}" Width="120"/>
                    <DataGridTextColumn Header="Provincia" Binding="{Binding Province}" Width="80"/>
                    <DataGridTextColumn Header="Fecha" Binding="{Binding Date}" Width="90"/>
                </DataGrid.Columns>

            </DataGrid>

        </DockPanel>
    </Border>
</UserControl>
