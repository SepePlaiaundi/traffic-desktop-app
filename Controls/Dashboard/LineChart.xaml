<UserControl x:Class="TrafficDesktopApp.Controls.Dashboard.LineChart"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf">

    <lvc:CartesianChart x:Name="Chart"
                        LegendLocation="None"
                        Background="Transparent"
                        Margin="0,10,0,0">

        <!-- EJE X -->
        <lvc:CartesianChart.AxisX>
            <lvc:Axis x:Name="AxisX"
                      Foreground="#888"
                      FontSize="10">
                <lvc:Axis.Separator>
                    <lvc:Separator IsEnabled="False"/>
                </lvc:Axis.Separator>
            </lvc:Axis>
        </lvc:CartesianChart.AxisX>

        <!-- EJE Y -->
        <lvc:CartesianChart.AxisY>
            <lvc:Axis Foreground="#888"
                      FontSize="11"
                      MinValue="0">
                <lvc:Axis.Separator>
                    <lvc:Separator Stroke="#EEE"
                                   StrokeThickness="1"/>
                </lvc:Axis.Separator>
            </lvc:Axis>
        </lvc:CartesianChart.AxisY>

    </lvc:CartesianChart>
</UserControl>
