<UserControl x:Class="TrafficDesktopApp.Controls.Dashboard.IncidentsList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Últimas incidencias"
                       FontSize="14"
                       FontWeight="SemiBold"
                       Margin="0,0,0,12"/>

        <Grid Grid.Row="1"
                  Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Incidencia" Foreground="#777"/>
            <TextBlock Text="Carretera" Grid.Column="1" Foreground="#777"/>
            <TextBlock Text="Provincia" Grid.Column="2" Foreground="#777"/>
        </Grid>

        <ItemsControl Grid.Row="2"
                          ItemsSource="{Binding Incidents}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="{Binding Type}"/>
                        <TextBlock Text="{Binding Road}"
                                       Grid.Column="1"/>
                        <TextBlock Text="{Binding Province}"
                                       Grid.Column="2"/>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

    </Grid>

</UserControl>
