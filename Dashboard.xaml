<Window x:Class="TrafficDesktopApp.Dashboard"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:general="clr-namespace:TrafficDesktopApp.Controls.General"
      xmlns:controls="clr-namespace:TrafficDesktopApp.Controls.Dashboard"
      Title="SEID - Dashboard"
      Width="1280"
      Height="1000"
      Background="#F7F7F7" >

    <Grid>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <general:Header x:Name="Header" Grid.Row="0" Title="Panel de control" />

        <ScrollViewer Grid.Row="1"
              VerticalScrollBarVisibility="Auto"
              HorizontalScrollBarVisibility="Disabled">

            <Grid Margin="20">
                <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <controls:DashboardCard
                        Grid.Column="0"
                        Margin="0,0,15,0"
                        Title="Incidencias hoy"
                        Value="—"
                        Subtitle="+ respecto al día anterior"/>

                    <controls:DashboardCard
                        Grid.Column="1"
                        Margin="0,0,15,0"
                        Title="Cámaras disponibles"
                        Value="—"
                        Subtitle="Última añadida —"/>

                    <controls:DashboardCard
                        Grid.Column="2"
                        Title="Usuarios"
                        Value="—"
                        Subtitle="Respecto al mes anterior"/>

                </Grid>

                <Grid Grid.Row="2">

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0"
                        Background="White"
                        BorderBrush="#DDD"
                        BorderThickness="1"
                        CornerRadius="6"
                        Padding="20"
                        Margin="0,0,20,0">

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <TextBlock Text="Incidencias en el último día"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Margin="0,0,0,12"/>

                            <controls:LineChart Grid.Row="1" Height="260"/>

                        </Grid>

                    </Border>

                    <controls:CamerasList Grid.Column="1"/>
                    
                </Grid>

                <Grid Grid.Row="4">
                    
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" MinWidth="520"/>
                        <ColumnDefinition Width="*" MinWidth="360"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0"
                        Background="White"
                        BorderBrush="#DDD"
                        BorderThickness="1"
                        CornerRadius="6"
                        Padding="20"
                        Margin="0,0,20,0">

                        <controls:IncidentsList Grid.Row="1"/>

                    </Border>

                    <Border Grid.Column="1"
                        Background="White"
                        BorderBrush="#DDD"
                        BorderThickness="1"
                        CornerRadius="6"
                        Padding="20">

                        <Grid>

                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <TextBlock Text="Incidencias en el último mes"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Margin="0,0,0,12"/>

                            <controls:MonthlyBarChart Grid.Row="1"/>

                        </Grid>

                    </Border>

                </Grid>

            </Grid>
            
        </ScrollViewer>
        
    </Grid>
             
</Window>
